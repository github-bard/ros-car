版本：
	version：2
	功能：实现了四电机的调速，编码读取
	变动：串口1改成了串口3，新增高级定时器1和8作编码器，新增通用定时器5作pwm
	不足：暂未实现定时器1和8读取数据的接口，串口的通信格式以及里程计均未适配四电机，单独轮控尚未实现（全向轮实现前提）
疑问：
	1、stm32f103可以驱动几个电机？
	2、一个定时器编码器模式有几个输入通道？   只能有两个ch1、ch2
	3、motor.c 速度控制函数为什么要+10000？
	4、一个while死循环执行不了一遍？        被中断处理函数打断
	5、串口助手如何发送指定字节数的数据？   1个字节=8个比特；用16进制发
	6、可不可以用一个定时器边读取数据边计算？应该不可以
	7、麦轮速度结解算
	8、中间层通信：无法按预想控制
	9、只在使用TIM3时，轮速有明显下降；使用TIM1、TIM8编码器无读取
	10、高级定时器有双边沿读取吗？
	11、中断向量和中断函数对应关系
	12、固件库手册中有TIM1_ITConfig，用的时候报错


学习：
	1、中断优先级的影响
	2、定时器模式内容及原理
	3、串口原理



注意：
	工程优化（函数形式及算法参数）
	精简函数
	按功能分库
	

区别：
	1、nvic.c中断优先级分组不同，flash没用
	2、rcc.c没用
	3、usart.c时钟复用没开
	4、pwm.c tim4周期设置不同、ARR未使能
	5、encoder.c tim6周期设置不同
	6、encoder.c 编码器没配置滤波
	

/***********************************************  说明  *****************************************************************
*
*   1.串口接收
*    （1）内容：小车左右轮速度,单位:mm/s（所有数据都为float型，float型占4字节）
*    （2）格式：10字节 [右轮速度4字节][左轮速度4字节][结束符"\r\n"2字节]
*
*   2.串口发送
*    （1）内容：里程计（x,y坐标、线速度、角速度和方向角，单位依次为：mm,mm,mm/s,rad/s,rad，所有数据都为float型，float型占4字节）
*    （2）格式：21字节 [x坐标4字节][y坐标4字节][方向角4字节][线速度4字节][角速度4字节][结束符"\n"1字节]
*
************************************************************************************************************************/
小端存储，串口内容先出低位后出高位